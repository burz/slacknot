<p>
  <strong>Name:</strong>
  <%= @assignment.name %>
</p>

<p>
  <strong>Assigned on:</strong>
  <%= @assignment.assigned_on %>
</p>

<p>
  <strong>Due on:</strong>
  <%= @assignment.due_on %>
</p>

<p>
  <strong>Percent completed:</strong>
  <%= @assignment.percent_completed %> %
</p>

<%= link_to 'Add problem',
      new_user_klass_assignment_problem_path(@user, @klass, @assignment) %> |
<%= link_to 'Edit', edit_user_klass_assignment_path(@user, @klass, @assignment) %> |
<%= link_to 'Back', user_klass_assignments_path(@user, @klass) %>
